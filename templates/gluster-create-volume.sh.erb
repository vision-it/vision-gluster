#!/bin/sh

set -e
set -x

<%= @gluster_binary %> \
    volume create <%= @volume_name %> \
    replica <%= @replica %> \
    arbiter <%= @arbiter %> \
    <%= @bricks.join(' ') %>

<%= @gluster_binary %> \
    volume start <%= @volume_name %>
